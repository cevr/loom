{
  "name": "@cvr/loom-code-kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/ --pass-with-no-tests"
  },
  "dependencies": {
    "@cvr/loom-platform-bun": "workspace:*",
    "@effect/platform-bun": "catalog:"
  },
  "peerDependencies": {
    "effect": "catalog:"
  }
}
